# UI 草稿转译

从草稿图整理出的骨架：

- 首屏：欢迎、开始新故事、档案、关于游戏。
- 故事界面：左侧故事/章节列表，右侧主内容区，下方命令输入。
- 终端界面：黑底输出、提示符 `>`、命令结果列表。
- 命令：`help`、`back`、`save`、`load`、`doc`、`open`、`find`、`note`、`link`、`rule`。
- 交互原则：命令本身也是谜题的一部分，初期只暴露少量命令，后续通过档案逐步解锁。

当前原型先保留骨架与交互，不锁定最终画风。
